/*
 * importMap.ts — Mapa de imports para el admin de Payload CMS.
 *
 * Payload usa este archivo para saber qué componentes cargar en el admin.
 * Es generado automáticamente por Payload al correr `next build` o `next dev`.
 *
 * Si ves errores relacionados a este archivo, corrí `npm run dev` una vez
 * para que Payload lo regenere con el contenido correcto.
 */

export const importMap = {}
